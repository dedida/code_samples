package imageprocessor.controller.commands;

import java.awt.event.ActionListener;

/**
 * This interface is for all the command classes and extends ActionListener. All the commands
 * implements this interface, so they can use ActionListener.
 */
public interface Commands extends ActionListener {

}
